// Reserved for future secure IPC bridges between the renderer and main
// process (contextBridge.exposeInMainWorld). Not needed yet: the renderer
// talks to the backend purely over HTTP (fetch to http://127.0.0.1:8000).
